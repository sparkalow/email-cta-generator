{
  "name": "email-cta-generator",
  "version": "0.1.0",
  "description": "Generate html email cta style buttons.",
  "dependencies": {
    "react": "^15.6.1",
    "react-color": "^2.13.0",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10"
  },
  "homepage": "https://sparkalow.github.io/email-cta-generator",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0"
  }
}
